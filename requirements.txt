# Runtime deps. If you are on Adobe's network and pip points at Artifactory,
# install from public PyPI:  pip install -r requirements.txt -i https://pypi.org/simple
pandas>=2.0
pyarrow>=14.0
numpy>=1.26
scikit-learn>=1.3
requests>=2.31
python-dotenv>=1.0
pydantic>=2.6
tenacity>=8.2
openai>=1.30
tqdm>=4.66
jinja2>=3.1

# web control panel (sss serve)
fastapi>=0.110
uvicorn>=0.27

# dev
pytest>=8.0
ruff>=0.5
